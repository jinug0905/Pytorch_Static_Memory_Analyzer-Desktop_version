# Pytorch_Static_Memory_Analyzer-Desktop_version
A work-in-progress tool to estimate inference peak GPU memory (parameters + activations) from TorchScript IR, and validate it against PyTorch CUDA allocator measurements.
